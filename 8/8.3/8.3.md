> [8. Aplicación de Patrones Arquitecturales](../8.md) › [8.3. Módulo 3 / Integrante 3](8.3.md)

# 8.3. Módulo 3 / Integrante 3

## Patrón de Módulo

**Título:** 

Elección del patrón de módulo para el módulo de Gestión de Usuarios y Seguridad

**Contexto:**

El módulo gestiona tanto usuarios internos como externos, manejando autenticación, permisos, roles, sesiones y registrar los logs. Requiere alta mantenibilidad, seguridad y separación clara entre la lógica de negocio, la presentación y la persistencia de datos.

**Alternativas:**

- **Arquitectura por capas**: Divide el sistema en capas (Presentación, Aplicación, Dominio, Infraestructura).

- **Arquitectura modular monolítica:** Mantener un monolito dividido internamente en módulos cohesivos.

**Criterios de elección:**

- Separación clara de responsabilidades.
- Facilidad de pruebas unitarias e integración.
- Escalabilidad futura sin complejidad inicial.
- Cumplimiento de atributos de calidad: mantenibilidad, testabilidad, seguridad.

**Decisión:**

Adoptar arquitectura por capas dentro de un monolito modular, donde el módulo de Gestión de Usuarios se compone de:

- Capa de Presentación (API/Login UI)
- Capa de Aplicación (Casos de uso: autenticación, registro, recuperación)
- Capa de Dominio (entidades, reglas de roles, validaciones de contraseña)
- Capa de Infraestructura (repositorios, integración con DB y servicios externos)

**Sustento:**

El patrón por capas facilita la separación lógica de la autenticación, los flujos de sesión y el registro de logs, permitiendo que el módulo mantenga alta mantenibilidad y testabilidad. Además, simplifica la evolución hacia servicios independientes si se requiere escalar.

--- 

## Patrón de Componente y Conector

**Título:** 

Elección para la interacción del módulo de usuarios

**Contexto:**

El módulo requiere comunicación entre el portal web (pacientes y personal clínico) y el backend de autenticación, además de notificar a otros módulos sobre inicios y cierres de sesión.

**Alternativas:**

- **Cliente-Servidor:** Comunicación directa vía API REST entre frontend y backend.

- **Publicar/Suscribir:** Emisión de eventos (login, logout, fallas) a un bus de mensajería (RabbitMQ/Kafka).

- **Modelo Vista-Controlador (MVC):** Separación entre interfaz y lógica, pero sin integración de eventos entre módulos.

**Criterios de elección:**

- Simplicidad para autenticación síncrona (tiempo real).

- Integración con otros módulos (Registro de Accesos).

- Soporte para eventos asíncronos de seguridad.

- Escalabilidad en escenarios con múltiples usuarios concurrentes.

**Decisión:**

Adoptar un modelo híbrido Cliente-Servidor + Publicar/Suscribir:

- Cliente-Servidor (REST): usado para autenticación, registro, gestión de sesiones y recuperación de contraseñas.

- Publicar/Suscribir (mensajería asíncrona): para emitir eventos de registro de accesos (login/logout).

**Sustento:**

Este patrón compuesto permite que las operaciones críticas (autenticación, login) se manejen de forma síncrona y confiable, mientras que las tareas de monitoreo de logs se gestionen asíncronamente, mejorando el rendimiento y la interoperabilidad (atributos definidos en RF6, RF10, RF13).

---

[⬅️ Anterior](../8.2/8.2.md) | [🏠 Home](../../README.md) | [Siguiente ➡️](../8.4/8.4.md)